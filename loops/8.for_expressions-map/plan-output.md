Changes to Outputs:
  + new-vpcs   = [
      + "database is the vpc for database",
      + "main is the main vpc",
    ]
  + new-vpcs-2 = {
      + Database = "Vpc For Database"
      + Main     = "Main Vpc"
    }